\documentclass[10pt,a4paper]{article}

% ---------- Geometry ----------
\usepackage[margin=2cm]{geometry}

% ---------- Fonts & layout ----------
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{setspace}
\setstretch{1.05}

% ---------- Lists ----------
\usepackage{enumitem}
\setlist[itemize]{
  leftmargin=*,
  itemsep=2pt,
  topsep=2pt,
  parsep=0pt,
  partopsep=0pt
}

% Pandoc compatibility
\providecommand{\tightlist}{%
  \setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}

% ---------- Headings ----------
\usepackage{titlesec}
\titleformat{\section}
  {\large\bfseries}
  {}
  {0pt}
  {}
\titlespacing*{\section}{0pt}{1.2em}{0.6em}

% ---------- Links ----------
\usepackage[hidelinks]{hyperref}

% ---------- Remove numbering ----------
\setcounter{secnumdepth}{0}

% ---------- No page numbers ----------
\pagestyle{empty}

% ---------- Kill horizontal rules ----------
\let\hrule\relax

% ---------- Document ----------
\begin{document}

% NOTE:
% We intentionally do NOT render name / email / links here.
% This PDF is a document, not a landing page.

$body$

\end{document}
